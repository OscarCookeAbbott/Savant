{
    "private": true,
    "name": "savant-monorepo",
    "type": "module",
    "version": "0.1.0",
    "scripts": {
        "dev": "cd packages/docs && vite",
        "build": "pnpm -r run build",
        "build:docs": "vite build --config packages/docs/vite.config.ts --outDir dist/docs"
    },
    "dependencies": {
        "@savant/core": "workspace:*",
        "@savant/routing": "workspace:*",
        "@savant/components": "workspace:*"
    }
}
